# The API the dashboard reads. No secret belongs in this file: the dashboard never holds
# a signing key, only the short-lived token the API issued to the person signed in.
NEXT_PUBLIC_VYRA_API_URL=http://localhost:8000
