{
  "name": "@meter402/x402",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "x402 protocol adapter. The only package that knows x402 wire format.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@meter402/payments": "workspace:*",
    "@meter402/shared": "workspace:*",
    "viem": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "@x402/core": "catalog:",
    "@x402/evm": "catalog:"
  }
}
