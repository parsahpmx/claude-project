{
  "name": "@meter402/example-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "An agent that meets a 402, decides, pays, and is served.",
  "scripts": {
    "start": "tsx src/agent.ts",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@meter402/client": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "^4.20.6",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
