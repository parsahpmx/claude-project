{
  "name": "@meter402/example-mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "An MCP server with a paid tool.",
  "scripts": {
    "start": "tsx src/server.ts",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@meter402/mcp": "workspace:*",
    "@meter402/sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "^4.20.6",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
